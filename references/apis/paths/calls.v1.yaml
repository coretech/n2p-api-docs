tags:
  - name: Calls
    description: Control your phone calls; List Active, Place, Answer, and Terminate your calls.
paths:
  /calls/:
    post:
      summary: Place a Call
      description: Place a call from the provided user/device to a given destination. Destination can be sip address or any valid phone number. E164 format is recommended.
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/call"
            examples:
              example:
                $ref: "#/components/examples/call"
      responses:
        201:
          description: Success
  /calls/{callId}/:
    delete:
      summary: Terminate an Active Call
      description: Terminate the call with the provided id
      parameters:
        - $ref: "#/components/parameters/callId"
      responses:
        204:
          description: Success
#  /calls/{callId}/answer/:
#    put:
#      summary: Answer a Call
#      description: Answer a call with a given id on a given device.
#      parameters:
#        - $ref: "#/components/parameters/callId"
#        - $ref: "#/components/parameters/to"
#      responses:
#        201:
#          description: Success
#  /calls/{callId}/reject/:
#    put:
#      summary: Reject a Call
#      description: Reject a call with a given id.
#      parameters:
#        - $ref: "#/components/parameters/callId"
#      responses:
#        201:
#          description: Success

    # TODO [i1] get example data
#  /calls/users/active/:
#    get:
#  /calls/users/{userId}/active/:
#    get:
