tags:
  - name: Number Portability
    description: Check if a given phone number is portable to Versature
paths:
  /phone_numbers/{phone_number}/portability/:
    get:
      parameters:
        - $ref: "#/components/parameters/phone_number"
      responses:
        200:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/portability"
              examples:
                example:
                  $ref: "#/components/examples/portability"
