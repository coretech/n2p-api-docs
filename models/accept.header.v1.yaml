type: string
name: Accept
required: true
in: header
description: The media type and version which is acceptable for the
enum:
  - application/vnd.integrate.v1.8.0+json
x-examples:
  example:
    application/vnd.integrate.v1.8.0+jsonresponse
